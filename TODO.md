* 右下に設定カテゴリをいれる。delete の確認をするかどうかのチェックボックス

## ショートカット周りで見つかった問題

* [x] 追加モード中でも ←/→ の画像送りが効いてしまい、塗りかけが失われる
* [x] 有効/無効の判定が二重管理(スロット側とボタン表示側で同じ条件を別々に計算)。
      `main_window._enabled` へ集約して QAction.setEnabled 駆動にした。
      `view.has_path()` を追えるよう ImageView に pathChanged を足してある
* [ ] Ctrl+Z が「パスの頂点取消」専用で、作図中でないと何も起きない。
      アプリ全体の undo が無いため、押しても無反応に見える
* [ ] Space / Backspace がウィンドウ全体のショートカットとして無条件に効く。
      今はテキスト入力欄が無いので実害は無いが、将来足すと入力を奪う
* [ ] Esc の2段階動作(パス破棄 -> モード取消 -> 選択解除)にテストが無い。
      Esc は常に有効で分岐が `_on_escape` の中にあるため、
      test_action_enabled.py の有効/無効テストでは捕まらない
* [ ] ショートカットをユーザーが変更できるようにする(第2段)。
      `shortcuts.Shortcut.id` を設定ファイルのキーにして keys を差し替える。
      表示は hint 経由なので追随する。キーの重複判定(test_shortcuts.py に
      あるもの)を、読み込み時にも通すこと
