* 右下に設定カテゴリをいれる。delete の確認をするかどうかのチェックボックス

## ショートカット周りで見つかった問題

* 追加モード中でも ←/→ の画像送りが効いてしまう。サイドパネルと一覧は
  `_on_add_mode_changed` で無効化されるのに、ショートカットは生きたまま。
  塗っている途中に画像が切り替わり、塗りが失われる
* 有効/無効の判定が二重管理。QAction は常に有効で各スロット冒頭で条件を再チェック
  して黙って return する一方、同じ条件をボタン表示側(`_update_top_bar` /
  `action_bar.set_active`)でも独立に計算しているためドリフトしうる。
  QAction.setEnabled 駆動へ寄せたいが、`_undo_path_point` の条件が
  `view.has_path()` に依存していて通知経路が無いので、その追加とセットで直す
  (上の ←/→ の件も同時に解決する)
